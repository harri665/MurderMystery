# Alternative docker-compose.yml for HTTPS domain
version: '3.8'

services:
  # Frontend (React/Vite)
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        - VITE_API_BASE=https://murdermystery.harrison-martin.com/api
    container_name: murdermystery-frontend
    ports:
      - "5080:80"
    environment:
      - VITE_API_BASE=https://murdermystery.harrison-martin.com/api
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - murdermystery-network

  # Backend (Express/Node.js)
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: murdermystery-backend
    ports:
      - "5081:3001"
    environment:
      - PORT=3001
      - ORIGIN=https://murdermystery.harrison-martin.com,http://murdermystery.harrison-martin.com
      - NODE_ENV=production
    volumes:
      # Persistent storage for data files - CRITICAL for data persistence
      - murdermystery_data:/app/data
    restart: unless-stopped
    networks:
      - murdermystery-network

volumes:
  # Named volume for persistent data storage
  murdermystery_data:
    driver: local

networks:
  murdermystery-network:
    driver: bridge