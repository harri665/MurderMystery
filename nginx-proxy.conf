upstream backend {upstream backend {

    server mysteryapi.harrison-martin.com:443;    server murdermystery-backend:3001;

}}



server {server {

    listen 80;    listen 80;

    server_name localhost;    server_name localhost;

        

    # Root directory for frontend files    # Root directory for frontend files

    root /usr/share/nginx/html;    root /usr/share/nginx/html;

    index index.html;    index index.html;



    # API routes - proxy to external backend    # API routes - proxy to backend

    location /api/ {    location /api/ {

        proxy_pass https://backend/api/;        proxy_pass http://backend/api/;

        proxy_set_header Host mysteryapi.harrison-martin.com;        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_ssl_verify off;        

                # Handle CORS

        # Handle CORS        add_header Access-Control-Allow-Origin * always;

        add_header Access-Control-Allow-Origin * always;        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;

        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization" always;

        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization" always;        

                if ($request_method = 'OPTIONS') {

        if ($request_method = 'OPTIONS') {            return 204;

            return 204;        }

        }    }

    }

    # Socket.IO WebSocket routes - proxy to backend

    # Socket.IO WebSocket routes - proxy to external backend    location /socket.io/ {

    location /socket.io/ {        proxy_pass http://backend/socket.io/;

        proxy_pass https://backend/socket.io/;        proxy_http_version 1.1;

        proxy_http_version 1.1;        proxy_set_header Upgrade $http_upgrade;

        proxy_set_header Upgrade $http_upgrade;        proxy_set_header Connection "upgrade";

        proxy_set_header Connection "upgrade";        proxy_set_header Host $host;

        proxy_set_header Host mysteryapi.harrison-martin.com;        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Real-IP $remote_addr;        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header X-Forwarded-Proto $scheme;    }

        proxy_ssl_verify off;

    }    # Frontend routes - serve static files and handle React Router

    location / {

    # Frontend routes - serve static files and handle React Router        try_files $uri $uri/ /index.html;

    location / {    }

        try_files $uri $uri/ /index.html;

    }    # Optional: Enable gzip compression

    gzip on;

    # Optional: Enable gzip compression    gzip_vary on;

    gzip on;    gzip_min_length 1024;

    gzip_vary on;    gzip_types

    gzip_min_length 1024;        text/plain

    gzip_types        text/css

        text/plain        text/xml

        text/css        text/javascript

        text/xml        application/javascript

        text/javascript        application/xml+rss

        application/javascript        application/json;

        application/xml+rss

        application/json;    # Security headers

    add_header X-Frame-Options "SAMEORIGIN" always;

    # Security headers    add_header X-XSS-Protection "1; mode=block" always;

    add_header X-Frame-Options "SAMEORIGIN" always;    add_header X-Content-Type-Options "nosniff" always;

    add_header X-XSS-Protection "1; mode=block" always;    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    add_header X-Content-Type-Options "nosniff" always;}

    add_header Referrer-Policy "no-referrer-when-downgrade" always;
}