{
  "scripts": {
    "dev:local": "Local development (both frontend and backend locally)",
    "dev:docker-backend": "Development with backend in Docker",
    "prod": "Production deployment with Docker Compose",
    "build": "Build for production"
  },
  "commands": {
    "dev:local": "npm start",
    "dev:docker-backend": "docker-compose -f docker-compose.dev.yml up -d && npm run dev",
    "prod": "docker-compose up -d",
    "build": "npm run build"
  }
}